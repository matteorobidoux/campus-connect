{
  "scripts": {
    "build": "cd client && npm install",
    "start": "export PORT=8080 && cd client && npm run start"
  },
  "dependencies": {
    "@types/react-calendar": "^3.9.0",
    "sass": "^1.58.0"
  }
}
